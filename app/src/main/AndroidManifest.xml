<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a3gproject_java">

    <!--인터넷권한설정-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.3GProject_Java"

        android:name=".KakaoApplication">


        <activity android:name=".Logout" />
        <activity android:name=".Change" />
        <activity android:name=".Checklist_jeongong" />
        <activity android:name=".Mypage" />
        <activity android:name=".Ppt" />
        <activity android:name=".Checklist" />
        <activity android:name=".Calculator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Redirect URI: "kakao{NATIVE_APP_KEY}://oauth" -->
                <data android:host="oauth"
                    android:scheme="kakao59d523ee90ee6c6976f371910563fd88" />
            </intent-filter>
        </activity>
        



    </application>

</manifest>